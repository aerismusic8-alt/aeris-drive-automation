# AX Quarantine

This directory is the non-destructive holding area for work that fails validation.

Do not delete quarantined evidence automatically.

Every item placed here must have enough metadata to identify the task, source, reason for quarantine, and verification state. Re-entry into an active queue requires validation after repair.
