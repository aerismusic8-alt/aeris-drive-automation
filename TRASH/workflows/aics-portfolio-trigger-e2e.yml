name: RETIRED - AICS Portfolio Trigger E2E

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retirement
        run: |
          echo "AICS portfolio execution is retired by K authority."
          echo "No XM live-trade execution is performed."
