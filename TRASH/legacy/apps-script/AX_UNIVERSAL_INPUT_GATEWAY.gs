/* Legacy Google Apps Script universal input gateway. Quarantined; not part of canonical runtime. */
const AX_INPUT_GATEWAY_VERSION = "1.0.0";
const AX_INPUT_SOURCE_TYPES = ["GOOGLE_DRIVE", "AERIS_DRIVE", "GITHUB", "CHATGPT_UPLOAD"];
function AX_INPUT_GATEWAY_STATUS() { return { system: "AX_UNIVERSAL_INPUT_GATEWAY", version: AX_INPUT_GATEWAY_VERSION, status: "ONLINE", policy: "CHATGPT_UPLOAD_NOT_REQUIRED", sources: AX_INPUT_SOURCE_TYPES, financialExecution: false, verified: true, timestamp: new Date().toISOString() }; }
function AX_REGISTER_INPUT_SOURCE(source) { if (!source || !source.type) throw new Error("INPUT_SOURCE_TYPE_REQUIRED"); const type = String(source.type).trim().toUpperCase(); if (AX_INPUT_SOURCE_TYPES.indexOf(type) === -1) throw new Error("UNSUPPORTED_INPUT_SOURCE: " + type); const reference = String(source.reference || "").trim(); if (!reference) throw new Error("INPUT_SOURCE_REFERENCE_REQUIRED"); return { accepted: true, sourceType: type, reference: reference, retrievalRequired: true, verified: true, timestamp: new Date().toISOString() }; }
function AX_VERIFY_INPUT_RETRIEVAL(result) { if (!result || result.retrieved !== true) return { verified: false, status: "INPUT_RETRIEVAL_NOT_VERIFIED", reason: "CONTENT_RETRIEVAL_REQUIRED" }; if (!result.contentHash || String(result.contentHash).trim() === "") return { verified: false, status: "INPUT_RETRIEVAL_NOT_VERIFIED", reason: "CONTENT_HASH_REQUIRED" }; return { verified: true, status: "INPUT_RETRIEVAL_VERIFIED", contentHash: String(result.contentHash), timestamp: new Date().toISOString() }; }
