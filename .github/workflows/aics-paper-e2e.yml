name: RETIRED - AICS Paper E2E

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retirement
        run: |
          echo "AICS paper execution is retired by K authority."
          echo "No AICS or XM execution is performed."
