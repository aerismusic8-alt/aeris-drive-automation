# XM Bridge Deployment Marker

The XM execution bridge is deployed through `.github/workflows/cloudflare-runtime.yml` from `main`.

Live financial execution remains disabled until the bridge readiness and explicit live-enable gates are satisfied.
